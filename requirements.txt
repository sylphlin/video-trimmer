# Core dependencies
google-genai>=0.1.1
google-cloud-storage>=3.0.0
google-auth>=2.0.0
requests>=2.31.0
soundfile>=0.12.1
numpy>=1.24.0
tenacity>=8.2.0

# Whisper ASR backends (at least one recommended)
# For macOS Apple Silicon (Metal GPU acceleration):
mlx-whisper>=0.4.0; sys_platform == 'darwin'

# For CPU / CUDA / general platforms:
faster-whisper>=1.0.0


